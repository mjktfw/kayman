#! /bin/bash

APP_NAME="kayman"
APP_PREFIX="kmn"

PROFILE_DIR="${HOME}/.profile.d"
BIN_DIR="${HOME}/.local/bin"
CONFIG_DIR="${HOME}/.config"

# TODO: directories for dependencies
MKS_DIR="${HOME}/lair/mkays"
