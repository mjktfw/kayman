#!/bin/bash

# GIT
GIT_EMAIL=""
GIT_USERNAME="mjktfw"

# GITHUB
GH_SCOPES="admin:public_key,admin:ssh_signing_key"

# KEY
KEY_TYPE="ed25519"
KEY_NAME="${USER}@${HOSTNAME}"
KEY_DIR="${HOME}/.ssh"
AUTH_KEYS="${KEY_DIR}/authorized_keys"
KEY_MAKE_DEFAULT=0

# PUBKEY
PUBKEY_USERNAME="mjktfw"
PUBKEY_REPO="mkays"
PUBKEY_REPO_DIR="${HOME}/lair/${PUBKEY_REPO}"
PUBKEY_REPO_IDENTITY=""
