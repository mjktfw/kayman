#! /bin/bash

KEY_NAME=""
GIT_EMAIL=""
GIT_USERNAME="mjktfw"

APP_NAME="kayman"
APP_PREFIX="kmn"

APP_BASEDIR="${MJK_BASEDIR}"
APP_DIR="${KMN_DIR}"

KEY_TYPE="ed25519"

PUBKEY_USERNAME="mjktfw"
PUBKEY_REPO="mkays"
PUBKEY_BASEDIR="${APP_BASEDIR}"
GH_SCOPES="admin:public_key,admin:ssh_signing_key"
