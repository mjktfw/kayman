#! /bin/bash

# TODO: rename steps
# TODO: divide variables into files relevant to steps (config, install, use, etc.)

# CONFIG
## GIT
GIT_EMAIL=""
GIT_USERNAME="mjktfw"

## GITHUB
GH_SCOPES="admin:public_key,admin:ssh_signing_key"

# USE
## KEY
KEY_TYPE="ed25519"
KEY_NAME="${HOSTNAME}"

## PUBKEY
# TODO: probably should be moved to another app
PUBKEY_USERNAME="mjktfw"
PUBKEY_REPO="mkays"
PUBKEY_BASEDIR="${APP_BASEDIR}"
